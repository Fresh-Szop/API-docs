type: object
properties:
    orders:
        type: array
        items:
            $ref: ":schema:/:OrderOverview"
    pages:
        $ref: ":schema:/:PagesCursor"
required:
    - orders
    - pages
