type: object
properties:
    recipes:
        type: array
        items:
            $ref: ":schema:/:RecipeOverview"
    pages:
        $ref: ":schema:/:PagesCursor"
required:
    - recipes
    - pages
